import { ActiveDeviceDescriptor } from './active-device-descriptor.model';

describe('ActiveDeviceDescriptor', () => {
  it('should create an instance', () => {
    expect(new ActiveDeviceDescriptor()).toBeTruthy();
  });
});
