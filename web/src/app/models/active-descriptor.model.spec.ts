import { ActiveDescriptor } from './active-descriptor.model';

describe('ActiveDescriptor', () => {
  it('should create an instance', () => {
    expect(new ActiveDescriptor()).toBeTruthy();
  });
});
