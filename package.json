{
    "name": "project2-telecomportal",
    "version": "1.0.0",
    "scripts": {
      "start": "npm run start --prefix web",
      "test": "npm run test --prefix web",
      "postinstall": "npm install --prefix web && npm install --prefix backend && npm run build --prefix web"
    },
    "cacheDirectories": [
      "web/node_modules",
      "backend/node_modules"
    ]
  }